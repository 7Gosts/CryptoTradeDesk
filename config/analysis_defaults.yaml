# CryptoTradeDesk 分析默认参数（可按需调整）
#
# 说明：
# - 脚本启动时会尝试读取本文件；读取失败则回退到代码内默认值。
# - 这里只放“相对稳定的策略/统计阈值”，避免把业务逻辑写死在代码里。
#
# 台账写入门槛（reward/risk）
min_journal_rr: 1.2

# 均线与窗口（需与图表 mav 口径一致）
ma_short: 8
ma_mid: 21
ma_long: 55

# 分形/结构参数
pivot_left: 2
pivot_right: 2
pivot_long_lookback_bars: 400
pivot_merge_rel: 0.001
pivot_touch_rel: 0.001
pivot_mtf_match_rel: 0.004

# 1-2-3 规则集
method_123_ruleset_id: ross_123_reversal_fractal_v1

# 飞书推送（可选）

FEISHU_APP_ID: "cli_a7defb0aacf8d00b"
FEISHU_APP_SECRET: "A8ETDmoj7bpabcxYQD7C7bfq26ACJjD7"
FEISHU_OPEN_ID: "ou_da40c916634649df3b447fd0c5a8d4e3"

# 自动通知的“强信号”阈值（可选；供 auto_4h_notify.py 默认读取）
auto_notify:
  min_score: 70
  min_wf: 55
  interval_hours: 1
  align_4h: true
